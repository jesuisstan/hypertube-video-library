'use client';

import BrowsePageComponent from '@/app/[locale]/(hypertube)/browse/browse-page-component';

const BrowseTop = () => {
  return <BrowsePageComponent category="top_rated" />;
};

export default BrowseTop;
